const labels={'static-custom':'Static Custom','food-series':'Food Series',spinning:'Spinning',wiggly:'Wiggly','simple-animated':'Simple Animated','custom-animated':'Custom Animated',sprites:'Sprites','pixel-headshots':'Pixel Headshots'};
function esc(s=''){return String(s).replace(/[&<>'"]/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[m]))}
async function copyName(a){if(!a.copyable)return;const text=`[[${a.title}]]`;try{await navigator.clipboard.writeText(text)}catch{const t=document.createElement('textarea');t.value=text;document.body.appendChild(t);t.select();document.execCommand('copy');t.remove()}showCopyToast(`${a.title} copied to clipboard`)}
function showCopyToast(message){let t=document.querySelector('#copy-toast');if(!t){t=document.createElement('div');t.id='copy-toast';t.className='copy-toast';document.body.appendChild(t)}t.textContent=message;t.classList.add('show');clearTimeout(window.__copyTimer);window.__copyTimer=setTimeout(()=>t.classList.remove('show'),1800)}
function windowControls(){return '<span class="window-dots"><i></i><i></i><i></i></span>'}
function card(a,index){const c=document.createElement('article');c.className='art-card pixel-window retro-window'+(a.copyable?' is-copyable':'');c.tabIndex=0;const media=a.file?`<img src="assets/art-web/${encodeURI(a.file)}" alt="${esc(a.title)}" draggable="false"><div class="art-watermark">@celerku</div>`:`<div class="art-placeholder"><span>✦</span><small>add art</small></div>`;const commissioner=a.commissioner?`<p class="commissioner">commissioner · ${esc(a.commissioner)}</p>`:'';const copyHint=a.copyable?`<span class="copy-hint">click card to copy [[name]]</span>`:`<span class="copy-hint disabled">copy disabled</span>`;c.innerHTML=`<div class="window-bar">${windowControls()}<span>${esc(labels[a.category]||a.category)}</span></div><div class="art-thumb">${media}</div><div class="art-info"><div><h3>${esc(a.title)}</h3><p>${esc(a.dimensions||'dimensions not set')}</p>${commissioner}</div><button class="view-art" type="button" ${a.file?'':'disabled'}>VIEW</button></div><div class="card-foot">${copyHint}</div>`;c.addEventListener('click',e=>{if(e.target.closest('.view-art'))return;copyName(a)});c.addEventListener('keydown',e=>{if((e.key==='Enter'||e.key===' ')&&!e.target.closest('.view-art')){e.preventDefault();copyName(a)}});c.querySelector('.view-art').addEventListener('click',e=>{e.stopPropagation();openLightbox(a,index)});return c}
function renderGalleries(){document.querySelectorAll('[data-gallery-category]').forEach(grid=>{const cat=grid.dataset.galleryCategory;const items=ARTWORKS.filter(a=>a.category===cat);grid.innerHTML='';if(!items.length){grid.innerHTML='<div class="empty-state">No artwork added here yet.</div>';return}items.forEach((a,index)=>grid.appendChild(card(a,index)))})}
function openLightbox(a,index){if(!a.file)return;const box=document.querySelector('#lightbox'),src=`assets/art-web/${a.file}`,actual=document.querySelector('#actual-img');document.querySelector('#lightbox-img').src=src;actual.src=src;if(a.nativeWidth&&a.nativeHeight){actual.style.width=`${a.nativeWidth}px`;actual.style.height=`${a.nativeHeight}px`}else{actual.style.width='auto';actual.style.height='auto'}document.querySelector('#lightbox-title').textContent=`${a.title} (${Number(index)+1})`;document.querySelector('#lightbox-meta').textContent=[a.dimensions,a.commissioner?`commissioner · ${a.commissioner}`:''].filter(Boolean).join(' · ');box.classList.add('open');box.setAttribute('aria-hidden','false')}
function closeLightbox(){const box=document.querySelector('#lightbox');box?.classList.remove('open');box?.setAttribute('aria-hidden','true')}
document.querySelector('.lightbox-close')?.addEventListener('click',closeLightbox);document.querySelector('#lightbox')?.addEventListener('click',e=>{if(e.target.id==='lightbox')closeLightbox()});document.addEventListener('keydown',e=>{if(e.key==='Escape')closeLightbox()});
renderGalleries();
